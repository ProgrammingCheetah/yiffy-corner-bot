# Secrets + runtime state: the vault never enters git.
config/vault/
*.sqlite
*.sqlite-journal
*.log

# Build output
node_modules/
dist/
/target/

# Webapp build artifacts (build/ itself IS committed so prod needs no node)
webapp/node_modules/
webapp/.svelte-kit/
.env
