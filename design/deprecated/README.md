# Deprecated Design Artifacts

These documents come from an earlier `code_migration/` workspace that has been wiped. They are kept here as historical reference only — current design work lives in `design/domain.md`.

- `DESIGN.md` — language-agnostic functional spec of the original TypeScript bot, written as a rewrite brief targeting Hexagonal Architecture. Superseded.
- `PLAN.md` — iterative rewrite plan that referenced `DESIGN.md`. Superseded.

Do not treat anything in this directory as authoritative. If a statement here conflicts with the current design, the current design wins.
